// app/layout.tsx
'use client'

import theme from '@/theme/theme'
export default function RootLayout({ children }: { children: React.ReactNode }) {
	return (
		<html lang="en">
			<head />
			<body>Test</body>
		</html>
	)
}
